# single collection name
COLLECTION=archivist

# ingestion strategy: simple | docling
INGESTION_STRATEGY=simple

# qdrant connection
QDRANT_HOST=localhost
QDRANT_PORT=6333

# embedding model (fastembed)
EMBEDDING_MODEL=BAAI/bge-small-en-v1.5
EMBEDDING_DIM=384

# server
HOST=0.0.0.0
PORT=8000
